import { Typography } from "@mui/material"
import { Router } from "../AppRouter"
import { Header } from "./Header/Header"


export const Home = () => {
    return(
        <Typography>
        Home Page
        </Typography>
    )
}